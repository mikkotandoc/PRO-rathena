# Block direct HTTP access to markdown source files
<IfModule mod_authz_core.c>
	<FilesMatch "\.md$">
		Require all denied
	</FilesMatch>
</IfModule>

<IfModule !mod_authz_core.c>
	<FilesMatch "\.md$">
		Order allow,deny
		Deny from all
	</FilesMatch>
</IfModule>
